# HalluciFix Environment Configuration Template
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Application Configuration
NODE_ENV=development
VITE_APP_NAME=HalluciFix
VITE_APP_VERSION=1.0.0
VITE_APP_URL=http://localhost:5173

# AWS Configuration (REQUIRED)
AWS_REGION=us-east-1
AWS_PROFILE=hallucifix

# AWS Cognito Authentication (REQUIRED)
VITE_AWS_REGION=us-east-1
VITE_AWS_USER_POOL_ID=us-east-1_XXXXXXXXX
VITE_AWS_USER_POOL_CLIENT_ID=your_cognito_client_id
VITE_AWS_IDENTITY_POOL_ID=us-east-1:xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
VITE_AWS_USER_POOL_DOMAIN=your-cognito-domain.auth.us-east-1.amazoncognito.com
VITE_AWS_OAUTH_REDIRECT_SIGN_IN=http://localhost:3000/callback
VITE_AWS_OAUTH_REDIRECT_SIGN_OUT=http://localhost:3000/logout

# AWS RDS Database (REQUIRED)
DATABASE_URL=postgresql://username:password@your-rds-endpoint:5432/hallucifix
DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
DB_PORT=5432
DB_NAME=hallucifix
DB_USERNAME=hallucifix_user
DB_PASSWORD=your_secure_password
DB_SSL=true
DB_MAX_CONNECTIONS=20

# AWS API Gateway (REQUIRED)
VITE_AWS_API_GATEWAY_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod

# AWS S3 Storage (REQUIRED)
VITE_AWS_S3_BUCKET=hallucifix-documents-prod

# AWS CloudFront CDN (REQUIRED)
VITE_CLOUDFRONT_DOMAIN=your-distribution.cloudfront.net

# =============================================================================
# AI SERVICES (Optional - enables real AI analysis)
# =============================================================================

# OpenAI Configuration
# Get your API key from: https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=sk-your_openai_api_key_here
VITE_OPENAI_MODEL=gpt-4
VITE_OPENAI_MAX_TOKENS=4000
VITE_OPENAI_TEMPERATURE=0.1

# HalluciFix Production API (for production deployments)
VITE_HALLUCIFIX_API_KEY=hf_your_hallucifix_api_key_here
VITE_HALLUCIFIX_API_URL=https://api.hallucifix.com

# =============================================================================
# AWS BEDROCK AI SERVICES (Optional - enables AWS AI analysis)
# =============================================================================

# AWS Bedrock Configuration
VITE_BEDROCK_ENABLED=true
VITE_BEDROCK_MODEL=anthropic.claude-3-sonnet-20240229-v1:0
VITE_BEDROCK_REGION=us-east-1

# AWS Credentials (Optional - uses IAM roles if not provided)
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# AWS Lambda Functions
LAMBDA_FUNCTION_PREFIX=hallucifix
VITE_LAMBDA_SCAN_EXECUTOR_NAME=hallucifix-scan-executor
VITE_LAMBDA_BILLING_API_NAME=hallucifix-billing-api

# AWS ElastiCache Redis
REDIS_CLUSTER_ENDPOINT=your-redis-cluster.cache.amazonaws.com:6379
REDIS_AUTH_TOKEN=your_redis_auth_token

# =============================================================================
# AUTHENTICATION & OAUTH (Optional - enables real authentication)
# =============================================================================

# Google OAuth Configuration
# Set up at: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=123456789-abc.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your_google_client_secret_here
VITE_GOOGLE_REDIRECT_URI=http://localhost:5173/auth/callback
VITE_OAUTH_LOGOUT_URI=http://localhost:5173/logout

# OAuth Security Configuration
OAUTH_TOKEN_ENCRYPTION_KEY=your_32_character_encryption_key_here
OAUTH_STATE_SECRET=your_oauth_state_secret_key_here
OAUTH_SESSION_SECRET=your_session_secret_key_here

# OAuth Scopes (space-separated)
GOOGLE_OAUTH_SCOPES=openid email profile https://www.googleapis.com/auth/drive.readonly

# OAuth Service Configuration
OAUTH_REFRESH_CHECK_INTERVAL_MS=300000
OAUTH_REFRESH_BUFFER_MS=300000
OAUTH_CLEANUP_INTERVAL_MS=3600000
OAUTH_TOKEN_GRACE_PERIOD_MS=86400000

# JWT Configuration (for custom auth)
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# =============================================================================
# PAYMENT PROCESSING (Optional - enables billing features)
# =============================================================================

# Stripe Configuration
# Get keys from: https://dashboard.stripe.com/apikeys
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Stripe Product Price IDs (set up in Stripe Dashboard)
STRIPE_PRICE_ID_BASIC_MONTHLY=price_basic_monthly_id
STRIPE_PRICE_ID_BASIC_YEARLY=price_basic_yearly_id
STRIPE_PRICE_ID_PRO_MONTHLY=price_pro_monthly_id
STRIPE_PRICE_ID_PRO_YEARLY=price_pro_yearly_id
STRIPE_PRICE_ID_API_CALLS=price_api_calls_id

# =============================================================================
# MONITORING & ANALYTICS (Optional - enables tracking and error reporting)
# =============================================================================

# Sentry Error Tracking
# Set up at: https://sentry.io/
VITE_SENTRY_DSN=https://your_sentry_dsn@sentry.io/project_id
SENTRY_AUTH_TOKEN=your_sentry_auth_token

# Google Analytics
VITE_GOOGLE_ANALYTICS_ID=GA_MEASUREMENT_ID

# Mixpanel Analytics
VITE_MIXPANEL_TOKEN=your_mixpanel_token

# =============================================================================
# FEATURE FLAGS (Optional - control feature availability)
# =============================================================================

# Enable/disable features
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_PAYMENTS=false
VITE_ENABLE_BETA_FEATURES=false
VITE_ENABLE_MOCK_SERVICES=true

# =============================================================================
# DEVELOPMENT & LOGGING (Optional - development configuration)
# =============================================================================

# Logging Configuration
LOG_LEVEL=info
LOG_FORMAT=pretty

# Log Aggregation Services
DATADOG_API_KEY=your_datadog_api_key_here
DATADOG_SITE=datadoghq.com
LOG_RETENTION_DAYS=30
LOG_MAX_SIZE_MB=100
ENABLE_LOG_AGGREGATION=false

# Webhook URLs (for development testing)
WEBHOOK_URL=https://your-domain.com/api/webhooks/stripe

# =============================================================================
# LEGACY SUPABASE CONFIGURATION (Migration Support Only)
# =============================================================================
# These variables are only needed during migration period
# Remove after migration is complete

# VITE_SUPABASE_URL=your_supabase_project_url
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
# SUPABASE_SERVICE_KEY=your_supabase_service_key

# =============================================================================
# QUICK SETUP GUIDE
# =============================================================================
#
# 1. MINIMUM SETUP (for basic functionality):
#    - Set AWS_REGION and AWS_PROFILE
#    - Set VITE_COGNITO_USER_POOL_ID and VITE_COGNITO_USER_POOL_CLIENT_ID
#    - Set DATABASE_URL or individual DB_* variables
#    - Keep VITE_ENABLE_MOCK_SERVICES=true for development
#
# 2. REAL AI ANALYSIS:
#    - Set VITE_BEDROCK_ENABLED=true and VITE_BEDROCK_MODEL
#    - OR set VITE_OPENAI_API_KEY for OpenAI
#    - Set VITE_ENABLE_MOCK_SERVICES=false
#
# 3. REAL AUTHENTICATION:
#    - Configure Cognito User Pool with Google OAuth
#    - Set VITE_GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET
#    - Configure OAuth redirect URIs in Google Console and Cognito
#
# 4. PAYMENT PROCESSING:
#    - Set Stripe keys and price IDs
#    - Set VITE_ENABLE_PAYMENTS=true
#
# 5. PRODUCTION DEPLOYMENT:
#    - Set NODE_ENV=production
#    - Configure all required AWS services
#    - Set up monitoring and analytics
#    - Deploy infrastructure using CDK
#
# =============================================================================